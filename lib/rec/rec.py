from filter import Filter
from text import Text
from stack import Stack 

class Rec( object ):
    def __init__(self):
        self.filter = Filter()
        self.text = Text()
        self.stack = Stack()
